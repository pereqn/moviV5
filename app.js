// OPTIMIZED: Safe stub for TelegramGameProxy
(() => {
  try {
    if (window && !window.TelegramGameProxy?.receiveEvent) {
      window.TelegramGameProxy = { receiveEvent: () => {} };
    }
  } catch(e) {}
})();
// full optimized JS code here
